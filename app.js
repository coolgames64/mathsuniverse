
const COURSE = [{"slug": "start-here", "file": "start-here.html", "title": "Start Here", "short": "Start", "level": "Orientation", "minutes": 10, "target": "Learn how to use the course, track progress, and study in the right order."}, {"slug": "numbers", "file": "numbers.html", "title": "Numbers and Place Value", "short": "Numbers", "level": "Foundation", "minutes": 15, "target": "Read, order, compare, and round whole numbers and negative numbers."}, {"slug": "addition", "file": "addition-subtraction.html", "title": "Addition and Subtraction", "short": "Addition", "level": "Foundation", "minutes": 15, "target": "Add, subtract, estimate, and check answers accurately."}, {"slug": "multiplication", "file": "multiplication-division.html", "title": "Multiplication and Division", "short": "Multiplication", "level": "Core", "minutes": 20, "target": "Use times tables, factors, multiples, arrays, and division facts."}, {"slug": "fractions", "file": "fractions.html", "title": "Fractions", "short": "Fractions", "level": "Core", "minutes": 20, "target": "Understand, simplify, compare, add, and subtract fractions."}, {"slug": "decimals", "file": "decimals.html", "title": "Decimals", "short": "Decimals", "level": "Core", "minutes": 20, "target": "Compare, round, add, subtract, and convert decimals."}, {"slug": "percentages", "file": "percentages.html", "title": "Percentages", "short": "Percentages", "level": "Core", "minutes": 20, "target": "Convert percentages and solve percentage-of-amount problems."}, {"slug": "ratio", "file": "ratio-proportion.html", "title": "Ratio and Proportion", "short": "Ratio", "level": "Higher", "minutes": 25, "target": "Simplify ratios, share amounts, and solve direct proportion problems."}, {"slug": "algebra", "file": "algebra.html", "title": "Algebra", "short": "Algebra", "level": "Higher", "minutes": 25, "target": "Simplify expressions, substitute values, and solve one-step and two-step equations."}, {"slug": "geometry", "file": "geometry.html", "title": "Geometry", "short": "Geometry", "level": "Core", "minutes": 25, "target": "Use shape facts, angle facts, perimeter, area, and simple diagrams."}, {"slug": "measurement", "file": "measurement.html", "title": "Measurement and Units", "short": "Units", "level": "Core", "minutes": 20, "target": "Convert units and solve measurement problems accurately."}, {"slug": "statistics", "file": "statistics.html", "title": "Statistics and Data", "short": "Statistics", "level": "Higher", "minutes": 25, "target": "Read charts and calculate mean, median, mode, and range."}, {"slug": "powers", "file": "powers-roots.html", "title": "Powers and Roots", "short": "Powers", "level": "Higher", "minutes": 25, "target": "Use squares, cubes, square roots, cube roots, and simple index notation."}, {"slug": "exam-trainer", "file": "exam-trainer.html", "title": "Exam Trainer", "short": "Exam", "level": "Final", "minutes": 30, "target": "Combine skills, show working, and answer exam-style questions clearly."}];
const STORAGE_KEY = 'mathsUniverseTeacherQuizProgressV1';
const TEACHER_ACCOUNT_KEY = 'mathsUniverseVerifiedTeacherAccountV2';
const TEACHER_SESSION_KEY = 'mathsUniverseVerifiedTeacherSessionV2';
const CERT_NAME_KEY = 'mathsUniverseCertificateNameV1';
const API_BASE = window.location.protocol === 'file:' ? 'http://localhost:3000' : '';
function isProbablyEmail(email){return /^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/.test(String(email||'').toLowerCase())}
function escapeHtml(str){return String(str||'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]))}
function getTeacherAccount(){try{return JSON.parse(localStorage.getItem(TEACHER_ACCOUNT_KEY))||null}catch(e){return null}}
function saveTeacherAccount(acc){localStorage.setItem(TEACHER_ACCOUNT_KEY,JSON.stringify(acc))}
function isTeacherLoggedIn(){const acc=getTeacherAccount();return localStorage.getItem(TEACHER_SESSION_KEY)==='true' && !!acc && acc.verified===true}
function teacherName(){const a=getTeacherAccount();return a?(a.name||'Teacher'):'Teacher'}
function setTeacherSession(on){if(on)localStorage.setItem(TEACHER_SESSION_KEY,'true');else localStorage.removeItem(TEACHER_SESSION_KEY)}
function simpleHash(text){let h=2166136261; for(let i=0;i<text.length;i++){h^=text.charCodeAt(i); h+=(h<<1)+(h<<4)+(h<<7)+(h<<8)+(h<<24);} return 'h'+(h>>>0).toString(16)}
function teacherUnlockAllProgress(){const s=getState(); s.completed=COURSE.map(t=>t.slug); s.questions={}; COURSE.forEach(t=>{s.questions[t.slug]={}; for(let i=1;i<=10;i++)s.questions[t.slug][i]=true;}); s.scores={}; COURSE.forEach(t=>s.scores[t.slug]=10); saveState(s); updateUI();}
function teacherClearStudentProgressOnly(){localStorage.removeItem(STORAGE_KEY); updateUI();}

function getState(){try{return JSON.parse(localStorage.getItem(STORAGE_KEY))||{completed:[],questions:{},scores:{}}}catch(e){return{completed:[],questions:{},scores:{}}}}
function saveState(s){localStorage.setItem(STORAGE_KEY,JSON.stringify(s))}
function indexOfCourse(slug){return COURSE.findIndex(t=>t.slug===slug)}
function item(slug){return COURSE.find(t=>t.slug===slug)}
function isDone(slug){return getState().completed.includes(slug)}
function prevItem(slug){const i=indexOfCourse(slug);return i>0?COURSE[i-1]:null}
function nextItem(slug){const i=indexOfCourse(slug);return i>=0&&i<COURSE.length-1?COURSE[i+1]:null}
function isLessonUnlocked(slug){if(isTeacherLoggedIn())return true; const i=indexOfCourse(slug); if(i<=0)return true; return isDone(COURSE[i-1].slug)}
function isQuestionDone(slug,n){const s=getState();return !!(s.questions[slug]&&s.questions[slug][n])}
function isQuestionUnlocked(slug,n){if(isTeacherLoggedIn())return true; if(!isLessonUnlocked(slug))return false; if(n===1)return true; return isQuestionDone(slug,n-1)}
function markQuestion(slug,n){const s=getState(); if(!s.questions[slug])s.questions[slug]={}; s.questions[slug][n]=true; saveState(s); updateUI();}
function markLessonComplete(slug){const s=getState(); if(!s.completed.includes(slug))s.completed.push(slug); let count=0; if(s.questions[slug]){for(let i=1;i<=10;i++) if(s.questions[slug][i]) count++;} s.scores[slug]=count; saveState(s); updateUI();}
function coursePercent(){const s=getState();return Math.round((s.completed.length/COURSE.length)*100)}
function updateUI(){const s=getState(); const pct=coursePercent(); const teacher=isTeacherLoggedIn(); document.querySelectorAll('[data-progress-text]').forEach(el=>el.textContent=teacher?'Teacher mode: all content open':s.completed.length+'/'+COURSE.length+' missions completed'); document.querySelectorAll('[data-progress-percent]').forEach(el=>el.textContent=teacher?'Teacher access':pct+'%'); document.querySelectorAll('[data-progress-bar]').forEach(el=>el.style.width=teacher?'100%':pct+'%'); document.querySelectorAll('[data-teacher-status]').forEach(el=>{el.textContent=teacher?'Teacher: '+teacherName():'Student mode'; el.className='teacher-status '+(teacher?'':'off')}); document.querySelectorAll('[data-teacher-banner]').forEach(el=>{el.classList.toggle('hidden',!teacher); const n=el.querySelector('[data-teacher-name]'); if(n)n.textContent=teacherName();}); document.querySelectorAll('[data-course-card]').forEach(card=>{const slug=card.dataset.courseCard;const unlocked=isLessonUnlocked(slug),done=isDone(slug);card.classList.toggle('locked',!unlocked && !teacher); card.classList.toggle('teacher-unlocked',teacher && !done);const st=card.querySelector('[data-card-status]'); if(st){st.textContent=done?'Completed':teacher?'Teacher unlocked':unlocked?'Unlocked':'Locked';st.className='badge '+(done?'done':teacher?'warn':unlocked?'':'lock')}}); document.querySelectorAll('[data-quiz-dots]').forEach(root=>{const slug=root.dataset.quizDots; let html=''; for(let i=1;i<=10;i++){html+='<span class="'+(isQuestionDone(slug,i)?'done ': '')+(String(i)===root.dataset.current?'current':'')+'">'+i+'</span>'} root.innerHTML=html;}); document.querySelectorAll('[data-next-lesson]').forEach(el=>{const next=teacher?COURSE[0]:COURSE.find(t=>!s.completed.includes(t.slug)); el.innerHTML=teacher?'<strong>Teacher mode:</strong> all lessons, quizzes, and teaching resources are open.':next?'<strong>Next mission:</strong> <a href="'+next.file+'">'+next.title+'</a>':'All missions completed. Certificate unlocked.'}); document.querySelectorAll('[data-completed-list]').forEach(el=>{if(teacher){el.innerHTML='<div class="study-task"><strong>Teacher mode is active.</strong><br>Every mission and quiz page is available for planning and teaching.</div>';return;} if(!s.completed.length){el.innerHTML='<div class="study-task">No missions completed yet. Start with Start Here.</div>';return;} el.innerHTML=s.completed.map(slug=>{const t=item(slug);return '<div class="study-task"><strong>'+(t?t.title:slug)+'</strong><br>10-question unlock quiz completed.</div>'}).join('')});}
function initNav(){const b=document.querySelector('[data-nav-toggle]'),l=document.querySelector('[data-nav-links]'); if(b&&l)b.addEventListener('click',()=>l.classList.toggle('open'))}
function lockLesson(){const slug=document.body.dataset.page; const main=document.querySelector('[data-lockable-main]'); if(isTeacherLoggedIn())return; if(!slug||!main||isLessonUnlocked(slug))return; const prev=prevItem(slug); main.innerHTML='<section class="container locked-screen card"><div class="lock-mark">L</div><h1>This mission is locked.</h1><p>Complete <strong>'+prev.title+'</strong> first. This keeps the learning path understandable and prevents gaps.</p><div class="hero-actions" style="justify-content:center"><a class="btn" href="'+prev.file+'">Go to required mission</a><a class="btn secondary" href="index.html#unlock-map">Back to map</a></div></section>';}
function initQuestionPage(){const root=document.querySelector('[data-question-page]'); if(!root)return; const slug=root.dataset.slug; const n=Number(root.dataset.number); const next=root.dataset.next; const pass=root.dataset.pass; const main=document.querySelector('[data-question-main]'); if(!isQuestionUnlocked(slug,n)){const prev=n===1?prevItem(slug):null; main.innerHTML='<section class="container locked-screen card"><div class="lock-mark">L</div><h1>Question '+n+' is locked.</h1><p>'+(n===1?'Complete the previous mission first.':'Answer Question '+(n-1)+' correctly first.')+'</p><div class="hero-actions" style="justify-content:center"><a class="btn" href="'+(n===1?(prev?(prev.file):'index.html'):(slug+'-q'+(n-1)+'.html'))+'">Go back</a></div></section>'; return;} const options=[...root.querySelectorAll('.option')]; const feedback=root.querySelector('[data-feedback]'); const nextBox=root.querySelector('[data-next-box]'); options.forEach(btn=>btn.addEventListener('click',()=>{if(root.dataset.answered==='true')return; const correct=btn.dataset.correct==='true'; if(correct){root.dataset.answered='true'; btn.classList.add('correct'); feedback.className='feedback good'; feedback.innerHTML='<strong>Correct.</strong> '+btn.dataset.explain; markQuestion(slug,n); if(n===10){markLessonComplete(slug); feedback.innerHTML+='<br><br><strong>Mission complete.</strong> The next mission is now unlocked.';} nextBox.classList.remove('hidden'); window.scrollTo({top:document.body.scrollHeight,behavior:'smooth'}); showToast(n===10?'Mission complete. Next lesson unlocked.':'Correct. Next question unlocked.');}else{btn.classList.add('wrong'); feedback.className='feedback bad'; feedback.innerHTML='<strong>Not quite.</strong> '+btn.dataset.explain+'<br>Read the hint, then try another option.'; feedback.classList.remove('hidden');}}));}
function showToast(msg){const t=document.createElement('div');t.className='toast';t.textContent=msg;document.body.appendChild(t);setTimeout(()=>t.remove(),2600)}
function initReset(){document.querySelectorAll('[data-reset-course]').forEach(b=>b.addEventListener('click',()=>{if(confirm('Reset all saved progress on this browser?')){localStorage.removeItem(STORAGE_KEY);location.reload();}}))}
function initPlanner(){const b=document.querySelector('[data-plan-button]'),out=document.querySelector('[data-study-plan]'); if(!b||!out)return; b.addEventListener('click',()=>{const s=getState(); const next=COURSE.find(t=>!s.completed.includes(t.slug))||COURSE[COURSE.length-1]; out.innerHTML='<div class="study-task"><strong>5 minutes:</strong> Read the Big Idea and Method Ladder in <a href="'+next.file+'">'+next.title+'</a>.</div><div class="study-task"><strong>10 minutes:</strong> Try the worked example and correct it.</div><div class="study-task"><strong>15 minutes:</strong> Complete quiz questions one at a time. Stop and repair if you get stuck.</div>';})}
function initSupportLocks(){if(isTeacherLoggedIn())return; const s=getState(); document.querySelectorAll('[data-requires-complete]').forEach(el=>{const req=el.dataset.requiresComplete;if(!s.completed.includes(req)){const t=item(req);el.innerHTML='<h3>Locked section</h3><p>Complete '+(t?t.title:req)+' to open this section.</p>';el.classList.add('locked')}})}
function initVault(){const root=document.querySelector('[data-formula-vault]');if(!root)return; const rows=[['Place value','Compare from left to right.','numbers'],['Subtraction check','answer + amount removed = starting number.','addition'],['Division check','quotient x divisor + remainder = original number.','multiplication'],['Fractions','Use a common denominator before adding or subtracting.','fractions'],['Percentage','percent means out of 100.','percentages'],['Ratio share','one part = total divided by total ratio parts.','ratio'],['Algebra','Do the same operation to both sides.','algebra'],['Rectangle area','area = length x width.','geometry'],['Metric conversion','m to cm: multiply by 100. kg to g: multiply by 1000.','measurement'],['Mean','total divided by number of values.','statistics'],['Square number','n squared = n x n.','powers']]; root.innerHTML=rows.map(r=>'<div class="formula-item" data-requires-complete="'+r[2]+'"><strong>'+r[0]+'</strong><span>'+r[1]+'</span></div>').join(''); initSupportLocks();}
function initCertificate(){const cert=document.querySelector('[data-certificate-content]'); if(!cert)return; const teacher=isTeacherLoggedIn(); if(!teacher && getState().completed.length<COURSE.length){cert.innerHTML='<div class="locked-screen card"><div class="lock-mark">L</div><h1>Certificate locked</h1><p>Complete every 10-question unlock quiz to open the certificate.</p><a class="btn" href="index.html#unlock-map">Return to course map</a></div>'; return;} const input=document.querySelector('[data-certificate-name-input]'); const nameEls=document.querySelectorAll('[data-certificate-name]'); const dateEls=document.querySelectorAll('[data-certificate-date]'); const modeEls=document.querySelectorAll('[data-certificate-mode]'); const saved=localStorage.getItem(CERT_NAME_KEY)||'Learner Name'; function renderName(v){nameEls.forEach(el=>el.textContent=v||'Learner Name')} if(input){input.value=saved==='Learner Name'?'':saved; input.addEventListener('input',()=>{const v=input.value.trim()||'Learner Name'; localStorage.setItem(CERT_NAME_KEY,v); renderName(v);})} renderName(saved); const d=new Date().toLocaleDateString(undefined,{year:'numeric',month:'long',day:'numeric'}); dateEls.forEach(el=>el.textContent=d); modeEls.forEach(el=>el.textContent=teacher?'Teacher preview certificate':'Verified completion certificate'); const list=document.querySelector('[data-certificate-missions]'); if(list){list.innerHTML=COURSE.map((t,i)=>'<li><span>'+(i+1)+'</span>'+escapeHtml(t.title)+'</li>').join('');}}
function initWorksheetSearch(){const input=document.querySelector('[data-worksheet-search]'); if(!input)return; const cards=[...document.querySelectorAll('[data-worksheet-card]')]; input.addEventListener('input',()=>{const q=input.value.toLowerCase(); cards.forEach(c=>c.style.display=c.textContent.toLowerCase().includes(q)?'':'none')});}

function initTeacherPortal(){const portal=document.querySelector('[data-teacher-portal]'); if(!portal)return; const requestBox=portal.querySelector('[data-request-box]'); const verifyBox=portal.querySelector('[data-verify-box]'); const loginBox=portal.querySelector('[data-login-box]'); const dashEls=portal.querySelectorAll('[data-teacher-dashboard]'); const msg=portal.querySelector('[data-auth-message]'); let pendingEmail=''; let pendingName=''; const show=(text,type='good')=>{if(msg){msg.className='auth-message '+type;msg.textContent=text;msg.classList.remove('hidden')}}; function render(){const acc=getTeacherAccount(); const logged=isTeacherLoggedIn(); if(requestBox)requestBox.classList.toggle('hidden',logged); if(verifyBox)verifyBox.classList.add('hidden'); if(loginBox)loginBox.classList.toggle('hidden',!acc||logged); dashEls.forEach(d=>d.classList.toggle('hidden',!logged)); const nameEl=portal.querySelector('[data-dashboard-name]'); if(nameEl&&acc)nameEl.textContent=acc.name||'Teacher'; const emailEl=portal.querySelector('[data-dashboard-email]'); if(emailEl&&acc)emailEl.textContent=acc.email?('('+acc.email+')'):''; const table=portal.querySelector('[data-teacher-course-table]'); if(table){table.innerHTML=COURSE.map((t,i)=>'<tr><td>'+(i+1)+'</td><td><strong>'+escapeHtml(t.title)+'</strong><br><span class="small">'+escapeHtml(t.target)+'</span></td><td><a href="'+t.file+'">Lesson</a></td><td><a href="quiz.html?topic='+t.slug+'&q=1">Quiz Q1</a></td><td>'+(t.slug==='start-here'?'Course intro':('<a href="'+t.slug+'-worksheet.html">Worksheet</a>'))+'</td></tr>').join('');} updateUI();}
  const requestForm=portal.querySelector('[data-request-code]'); if(requestForm)requestForm.addEventListener('submit',async e=>{e.preventDefault(); const fd=new FormData(requestForm); const name=(fd.get('name')||'').toString().trim(); const email=(fd.get('email')||'').toString().trim().toLowerCase(); if(name.length<2)return show('Please enter a teacher name.','bad'); if(!email)return show('Teacher email is required.','bad'); if(!isProbablyEmail(email))return show('Invalid email. Please enter a real teacher email address.','bad'); const btn=requestForm.querySelector('button[type="submit"]'); if(btn)btn.disabled=true; try{const res=await fetch(API_BASE+'/api/request-teacher-code',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email})}); const data=await res.json().catch(()=>({})); if(!res.ok)throw new Error(data.error||'Could not send the confirmation code.'); pendingEmail=email; pendingName=name; if(verifyBox)verifyBox.classList.remove('hidden'); show('Confirmation code sent to '+email+'. Enter the code to unlock Teacher Mode.'); const codeInput=portal.querySelector('[name="code"]'); if(codeInput)codeInput.focus();}catch(err){show(String(err.message||'').includes('fetch')?'Email verification server is not running. Start the included server, then try again.':err.message,'bad');}finally{if(btn)btn.disabled=false;}});
  const verifyForm=portal.querySelector('[data-verify-code]'); if(verifyForm)verifyForm.addEventListener('submit',async e=>{e.preventDefault(); const code=(new FormData(verifyForm).get('code')||'').toString().trim(); if(!/^\d{6}$/.test(code))return show('Enter the 6-digit confirmation code.','bad'); const btn=verifyForm.querySelector('button[type="submit"]'); if(btn)btn.disabled=true; try{const res=await fetch(API_BASE+'/api/verify-teacher-code',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:pendingEmail,code})}); const data=await res.json().catch(()=>({})); if(!res.ok)throw new Error(data.error||'Code not accepted.'); saveTeacherAccount({name:pendingName,email:pendingEmail,verified:true,verifiedAt:new Date().toISOString(),serverVerified:!!data.verified}); setTeacherSession(true); verifyForm.reset(); show('Email verified. Teacher Mode is now unlocked on this browser.'); render();}catch(err){show(err.message,'bad');}finally{if(btn)btn.disabled=false;}});
  const loginForm=portal.querySelector('[data-login-teacher]'); if(loginForm)loginForm.addEventListener('submit',e=>{e.preventDefault(); const acc=getTeacherAccount(); if(!acc||!acc.email)return show('No verified teacher account is saved on this browser. Request a new email code.','bad'); pendingEmail=acc.email; pendingName=acc.name; if(verifyBox)verifyBox.classList.remove('hidden'); show('For safety, request a fresh confirmation code for '+acc.email+' using the form above.','bad');});
  portal.querySelectorAll('[data-teacher-logout]').forEach(b=>b.addEventListener('click',()=>{setTeacherSession(false); show('Teacher mode is off. Student locks are active again.'); render();}));
  portal.querySelectorAll('[data-teacher-unlock-all]').forEach(b=>b.addEventListener('click',()=>{teacherUnlockAllProgress(); show('All missions and quiz questions are marked complete on this browser.'); render();}));
  portal.querySelectorAll('[data-teacher-clear-progress]').forEach(b=>b.addEventListener('click',()=>{if(confirm('Clear student progress on this browser? The verified teacher account will remain.')){teacherClearStudentProgressOnly(); show('Student progress cleared. Teacher account remains.'); render();}}));
  portal.querySelectorAll('[data-delete-teacher-account]').forEach(b=>b.addEventListener('click',()=>{if(confirm('Delete the verified teacher account from this browser?')){localStorage.removeItem(TEACHER_ACCOUNT_KEY);localStorage.removeItem(TEACHER_SESSION_KEY);teacherClearStudentProgressOnly(); show('Verified teacher account deleted from this browser.','good'); render();}})); render();}

document.addEventListener('DOMContentLoaded',()=>{initNav();lockLesson();initQuestionPage();initReset();initPlanner();initVault();initSupportLocks();initCertificate();initWorksheetSearch();initTeacherPortal();updateUI();});
